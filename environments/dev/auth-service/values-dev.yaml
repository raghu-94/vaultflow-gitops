replicaCount: 1
image:
  repository: 274394226013.dkr.ecr.ap-south-1.amazonaws.com/auth-service  # same ECR repo as prod
  tag: ""    # CI injects git SHA here
resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits:   { cpu: 200m, memory: 128Mi }
version: blue
